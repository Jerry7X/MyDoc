多分支站点的同步方案

需要支持如下场景：
1，支持单向同步。
2，支持外部数据源
   在配置了数据源的情况，如果请求对象在本地ENOENT，那么就发起请求去源上获取。
   支持多种数据源(考虑支持NAS的情况)。拉取回来的数据可以配置生命周期策略。
3，如果分支站点各使用不同的bucket(建议如此)，可以针对某些bucket设置。
   这样生命周期策略也比较好配置
   
   
风险：
1，用户希望在存储提供统一视图，可能会频繁在存储上执行list类操作，list本身比较慢，消耗资源，同时因为并发会导致其他操作也比较慢。